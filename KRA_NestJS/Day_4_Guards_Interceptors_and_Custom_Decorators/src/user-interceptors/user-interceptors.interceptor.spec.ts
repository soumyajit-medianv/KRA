import { UserInterceptorsInterceptor } from './user-interceptors.interceptor';

describe('UserInterceptorsInterceptor', () => {
  it('should be defined', () => {
    expect(new UserInterceptorsInterceptor()).toBeDefined();
  });
});
